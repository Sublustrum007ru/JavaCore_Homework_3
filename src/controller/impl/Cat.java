package controller.impl;

import controller.Animal;

public class Cat extends Animal {
    public Cat(String type, String name, float maxJump, float maxRun, float maxSwim) {
        super(type, name, maxJump, maxRun, maxSwim);
    }
}
